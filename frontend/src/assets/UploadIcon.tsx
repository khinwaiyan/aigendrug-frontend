import { IoCloudUploadOutline } from "react-icons/io5";

export const UploadIcon = ({
  size,
  color,
}: {
  size: number;
  color: string;
}) => {
  return <IoCloudUploadOutline size={size} color={color} />;
};
